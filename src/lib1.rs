

use std::collections::BTreeMap;


struct Config_er{
    active_moduals: list
    pub mut moduals_present: BTreeMap
}

let mut BTreeMap::new();

imp Add for config_er;


use std::fs;

pub trait Loadable{
    type Item;
    type status;
    fn load(&mut self) ->int;

}

pub struct config_file{
    String: file_path,

}

impl Loadable for config_file{

    type Item =
    type status = u32;
}

pub struct IO
{

}

fn somefunc() {

}



fn load_config_file(whichfile)


fn locate_files()
